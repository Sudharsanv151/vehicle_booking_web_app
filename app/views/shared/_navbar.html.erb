<nav>
  <div style="display: flex; justify-content: space-between; align-items: center;">
    <div>
      <%= link_to "RideBooker", home_path %>
    </div>

    <div>
      <% if session[:user_id] %>
        <% user = User.find(session[:user_id]) %>
        <% if user.userable_type == "Customer" %>
          <%= link_to "Browse Vehicles", home_path%> 
          <%= link_to "My Bookings", home_path %>
        <% else %>
          <%= link_to "My Vehicles", home_path %>
          <%= link_to "Booking Requests", home_path %>
        <% end %>
        <%= link_to "Logout", select_role_path, method: :get %>
      <% else %>
        <%= link_to "Login / Sign Up", select_role_path %>
      <% end %>
    </div>
  </div>
</nav>
