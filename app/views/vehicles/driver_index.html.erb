<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage My Vehicles</title>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-900 text-gray-100">

<div class="max-w-7xl mx-auto px-4 py-4 sm:py-8">
  <h2 class="text-2xl sm:text-3xl font-extrabold text-white text-center mb-6">Manage My Vehicles</h2>

  <!-- 🔍 Filter/Search Section -->
  <div class="bg-gradient-to-br from-gray-800 via-gray-800 to-gray-800 border border-gray-700 rounded-2xl shadow-lg p-6 mb-6 text-gray-100 transform hover:shadow-[0_0_25px_rgba(56,189,248,0.25)] transition-all duration-300">
    <div class="flex flex-col md:flex-row md:justify-between md:items-end gap-4">

      <%= form_with url: driver_vehicles_path, method: :get, local: true, class: "flex flex-wrap gap-3 items-end w-full md:w-auto" do %>
        <div class="flex-grow">
          <%= label_tag :query, "Search by Model", class: "block text-sm font-semibold text-gray-200 mb-1" %>
          <%= text_field_tag :query, params[:query], placeholder: "e.g., Sedan",
              class: "bg-gray-800 border border-gray-600 rounded-lg px-3 py-2 text-sm w-full text-gray-100 placeholder-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" %>
        </div>
        <div>
          <%= submit_tag "Search", class: "bg-green-600 hover:bg-green-700 text-white px-5 py-2 rounded-lg text-sm font-semibold shadow-md hover:shadow-green-500/30 transform hover:scale-105 transition duration-300 ease-in-out cursor-pointer" %>
        </div>
      <% end %>

      <%= form_with url: driver_vehicles_path, method: :get, local: true, class: "flex flex-wrap gap-3 items-end w-full md:w-auto" do %>
        <div>
          <%= label_tag :vehicle_type, "Type", class: "block text-sm font-semibold text-gray-200 mb-1" %>
          <%= select_tag :vehicle_type, options_for_select(@types.unshift("All"), params[:vehicle_type]), include_blank: true,
              class: "bg-gray-800 border border-gray-600 rounded-lg px-3 py-2 text-sm w-36 text-gray-100 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" %>
        </div>

        <div>
          <%= label_tag :tag_id, "Tag", class: "block text-sm font-semibold text-gray-200 mb-1" %>
          <%= select_tag :tag_id, options_from_collection_for_select(@tags, :id, :name, params[:tag_id]), include_blank: "All",
              class: "bg-gray-800 border border-gray-600 rounded-lg px-3 py-2 text-sm w-36 text-gray-100 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" %>
        </div>

        <div>
          <%= label_tag :min_rating, "Min Rating", class: "block text-sm font-semibold text-gray-200 mb-1" %>
          <%= number_field_tag :min_rating, params[:min_rating], in: 1..5, step: 0.1,
              class: "bg-gray-800 border border-gray-600 rounded-lg px-3 py-2 text-sm w-24 text-gray-100 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" %>
        </div>

        <div>
          <%= submit_tag "Filter", class: "bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-lg text-sm font-semibold shadow-md hover:shadow-blue-500/30 transform hover:scale-105 transition duration-300 ease-in-out cursor-pointer" %>
        </div>
      <% end %>

    </div>
  </div>

  <%= link_to "Add a New Vehicle", new_vehicle_path, class: "inline-block bg-green-600 hover:bg-green-700 text-white font-semibold py-2.5 px-6 rounded-lg shadow-md hover:shadow-purple-500/30 transform hover:-translate-y-0.5 transition duration-300 ease-in-out mb-6 no-underline" %>

  <% if @vehicles.blank? %>
    <div class="text-center p-6 bg-gray-800 rounded-2xl border border-gray-700 shadow-lg">
      <svg class="mx-auto h-10 w-10 text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
      <p class="text-gray-300 text-base font-medium mb-4">You haven't added any vehicles yet!</p>
      <%= link_to "Add Your First Vehicle", new_vehicle_path, class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded-lg shadow-md hover:shadow-blue-500/30 transform hover:scale-105 transition duration-300 ease-in-out no-underline" %>
    </div>
  <% else %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4">
      <% @vehicles.each do |vehicle| %>
        <div class="bg-gradient-to-br from-gray-800 via-gray-900 to-gray-800 border border-gray-700 rounded-2xl shadow-lg overflow-hidden transform hover:scale-[1.02] hover:shadow-2xl transition-all duration-300 text-sm text-gray-200">
          <div class="relative group">
            <% if vehicle.image.attached? %>
              <div class="overflow-hidden relative">
                <%= image_tag vehicle.image, class: "w-full h-44 object-cover transition-transform duration-500 group-hover:scale-105 border-b border-gray-700" %>
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-70 group-hover:opacity-80 transition-opacity duration-300 rounded-t-2xl"></div>
              </div>
            <% else %>
              <div class="w-full h-44 bg-gray-700 flex items-center justify-center text-gray-300 text-sm border-b border-gray-600 rounded-t-2xl">
                🚘 No Image Available
              </div>
            <% end %>
          </div>

          <div class="p-4 flex flex-col flex-1 space-y-2">
            <h3 class="text-base font-semibold text-gray-100 leading-snug">
              🚗 <%= vehicle.model.capitalize %>
              <span class="text-sm font-normal text-gray-400">(<%= vehicle.vehicle_type %>)</span>
            </h3>

            <p class="text-sm text-gray-300 leading-tight">
              🧍‍♂️ <strong>Capacity:</strong> <%= vehicle.capacity %> Passengers
            </p>

            <% if vehicle.tags.any? %>
              <div class="flex flex-wrap gap-1 mb-2">
                <% vehicle.tags.each do |tag| %>
                  <span class="bg-gray-700/60 text-blue-300 text-xs px-2 py-0.5 rounded-full font-medium border border-gray-600">
                    #<%= tag.name %>
                  </span>
                <% end %>
              </div>
            <% end %>

            <div class="flex flex-col space-y-1 mt-auto">
              <%= link_to "✏️ Edit Vehicle", edit_vehicle_path(vehicle), class: "text-blue-400 hover:text-blue-300 font-medium no-underline text-sm transition" %>
              <%= button_to "🗑️ Delete Vehicle", vehicle_path(vehicle), method: :delete, form: { onsubmit: "return confirm('Are you sure you want to delete this vehicle?');", class: "inline-block w-full" }, class: "text-red-400 hover:text-red-300 font-medium bg-transparent border-none p-0 cursor-pointer text-left text-sm transition" %>
              <%= link_to "⭐ View Ratings", vehicle_ratings_path(vehicle), class: "text-gray-400 hover:text-gray-300 font-medium no-underline text-sm transition" %>
              <%= link_to "🧾 View Ride History", vehicle_bookings_path(vehicle), class: "text-gray-400 hover:text-gray-300 font-medium no-underline text-sm transition" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="flex justify-center items-center gap-3 mt-6">
      <% if @vehicles.prev_page %>
        <%= link_to "← Previous", url_for(page: @vehicles.prev_page), class: "px-4 py-2 rounded-lg border border-gray-700 bg-gray-800 text-gray-300 hover:bg-gray-700 hover:border-gray-600 transition duration-200 font-medium text-sm" %>
      <% end %>

      <%= paginate @vehicles %>

      <% if @vehicles.next_page %>
        <%= link_to "Next →", url_for(page: @vehicles.next_page), class: "px-4 py-2 rounded-lg border border-gray-700 bg-gray-800 text-gray-300 hover:bg-gray-700 hover:border-gray-600 transition duration-200 font-medium text-sm" %>
      <% end %>
    </div>
  <% end %>
</div>
</body>
</html>
