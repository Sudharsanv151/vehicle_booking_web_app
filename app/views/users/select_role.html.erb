<div class="flex flex-col items-center justify-center min-h-screen bg-gray-100 p-6">
  <h1 class="text-4xl font-extrabold text-gray-800 mb-8 text-center">Select Your Role</h1>

  <div class="flex flex-col space-y-6 w-full max-w-sm">
    <%= link_to "I'm a Customer", new_user_session_path(role: 'customer') %>
    <%= link_to "I'm a Driver", new_user_session_path(role: 'driver') %>
  </div>
</div>