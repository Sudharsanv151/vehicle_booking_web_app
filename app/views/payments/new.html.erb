<div class="flex flex-col items-center justify-center min-h-screen bg-gray-50 p-6">
  <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-md border border-gray-200">
    <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">Make Payment for Booking</h1>

    <% if @booking %>
      <p class="text-gray-700 text-center mb-6">
        Total Price: <span class="font-semibold text-blue-600">₹<%= @booking.price %></span>
      </p>

      <%= form_with model: @payment, local: true, class: "space-y-6" do |f| %>
        <%= f.hidden_field :booking_id, value: @booking.id %>

        <div>
          <%= f.label :payment_type, "Payment Type:", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.select :payment_type, ["Cash", "Card", "UPI"], {}, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
        </div>

        <%= f.submit "Pay Now", class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2.5 px-4 rounded-md shadow-md text-lg focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200 cursor-pointer" %>
      <% end %>
    <% else %>
      <div class="text-center p-6 bg-red-50 border border-red-200 text-red-700 rounded-lg shadow-sm">
        <p class="mb-4">Booking not found. Please return to your dashboard.</p>
        <%= link_to "Go to Dashboard", home_path, class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md no-underline transition-colors duration-200" %>
      </div>
    <% end %>
  </div>
</div>